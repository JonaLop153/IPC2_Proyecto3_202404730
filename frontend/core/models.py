from django.db import models

# No necesitamos modelos de base de datos en Django ya que usaremos el backend Flask
# Este archivo puede quedar vacío o contener modelos si se necesita persistencia local